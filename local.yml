- hosts: localhost
  connection: localhost
  become: true

  pre_tasks:
    - name: update repos
      apt:
        update_cache: true
      changed_when: False
  
  tasks:
    - include: tasks/initial_packages.yml
    - include: tasks/create_users.yml
    - include: tasks/cron.yml
    